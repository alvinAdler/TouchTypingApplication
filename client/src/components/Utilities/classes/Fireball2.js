import Sprite2 from "./Sprite2"

class Fireball2 extends Sprite2{
    constructor(main, spriteSheet, spriteData, initPositions, initVelocities, initDir, frameCounter, idleSprite){
        super(main, spriteSheet, spriteData, initPositions, initVelocities, initDir, frameCounter, idleSprite)
    }
}

export default Fireball2